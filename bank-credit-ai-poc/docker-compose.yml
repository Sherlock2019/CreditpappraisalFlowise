services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: credit_ai_postgres
    environment:
      POSTGRES_DB: credit_ai
      POSTGRES_USER: credit_ai_user
      POSTGRES_PASSWORD: credit_ai_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

  backend:
    build: ./backend
    container_name: credit_ai_backend
    env_file:
      - .env
    environment:
      FLOWISE_API_URL: http://host.docker.internal:3001
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      DEMO_CUSTOMER_DOCUMENTS_DIR: /app/demo/customer_documents
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    volumes:
      - ./data/uploads:/app/data/uploads
      - ../docfactor_banking_demo_dataset/customer_documents:/app/demo/customer_documents:ro

  frontend:
    build: ./frontend
    container_name: credit_ai_frontend
    environment:
      BACKEND_URL: http://backend:8000
    ports:
      - "8501:8501"
    depends_on:
      - backend

  flowise:
    image: flowiseai/flowise:3.1.2
    container_name: credit_ai_flowise
    profiles:
      - flowise
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    volumes:
      - flowise_data:/root/.flowise

volumes:
  postgres_data:
  flowise_data:
